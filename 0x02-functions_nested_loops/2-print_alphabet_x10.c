#include "main.h"
/**
 * print_alphabet_x10 - printing alphabet x10 times
 * Return: void
 */
void print_alphabet_x10(void)
{
	char alpha;
	int a = 0;

	while (a <= 0)
	{
		for (alpha = 'a' ; alpha <= 'z'; alpha++)
		{
			_putchar(alpha);
		}
		_putchar('\n');
		a++;
	}
}
