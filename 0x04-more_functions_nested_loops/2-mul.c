#include "main.h"
/**
 * mul - Muliplies the tow integers
 * @x:the first integer
 * *@y: the second integer
 *
 * return: The result of the multiplication
 */
int mul(int x, int y)
{
	return (x * y);
}
